
## README

## Available Scripts

In the project directory, you can run:

### `npm run generate`

This command is required to generate an input array. Or copy to a directory a file called "sharesArray.json"

### `npm run start`

This command runs a script that generates an outgoing array and writes data to a file called "percentageSharesArray.json"

